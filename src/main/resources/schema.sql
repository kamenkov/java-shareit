CREATE TABLE IF NOT EXISTS app_user
(
    id    BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name  VARCHAR(255)                            NOT NULL,
    email VARCHAR(255) UNIQUE                     NOT NULL,
    CONSTRAINT pk_app_user PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS item
(
    id           BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name         VARCHAR(255) NOT NULL,
    description  VARCHAR(255) NOT NULL,
    is_available BOOLEAN NOT NULL,
    owner_id     BIGINT NOT NULL,
    item_request_id BIGINT,
    CONSTRAINT pk_item PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS booking
(
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    user_id    BIGINT NOT NULL,
    start_date TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    end_date   TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    item_id    BIGINT NOT NULL,
    status     INTEGER NOT NULL,
    CONSTRAINT pk_booking PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS comment
(
    id        BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    text      VARCHAR(255)                            NOT NULL,
    author_id BIGINT NOT NULL,
    item_id   BIGINT NOT NULL,
    created   TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    CONSTRAINT pk_comment PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS item_request
(
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    description VARCHAR(255)                            NOT NULL,
    created     TIMESTAMP WITHOUT TIME ZONE             NOT NULL,
    creator_id  BIGINT                                  NOT NULL,
    CONSTRAINT pk_item_request PRIMARY KEY (id)
);

ALTER TABLE item_request
    ADD CONSTRAINT FK_ITEM_REQUEST_ON_CREATOR FOREIGN KEY (creator_id) REFERENCES app_user (id);

ALTER TABLE comment
    DROP CONSTRAINT IF EXISTS FK_COMMENT_ON_AUTHOR;
ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_AUTHOR FOREIGN KEY (author_id) REFERENCES app_user (id);

ALTER TABLE comment
    DROP CONSTRAINT IF EXISTS FK_COMMENT_ON_ITEM;
ALTER TABLE comment
    ADD CONSTRAINT FK_COMMENT_ON_ITEM FOREIGN KEY (item_id) REFERENCES item (id);

ALTER TABLE booking
    DROP CONSTRAINT IF EXISTS FK_BOOKING_ON_ITEM;
ALTER TABLE booking
    ADD CONSTRAINT FK_BOOKING_ON_ITEM FOREIGN KEY (item_id) REFERENCES item (id);

ALTER TABLE booking
    DROP CONSTRAINT IF EXISTS FK_BOOKING_ON_USER;
ALTER TABLE booking
    ADD CONSTRAINT FK_BOOKING_ON_USER FOREIGN KEY (user_id) REFERENCES app_user (id);

ALTER TABLE item
    DROP CONSTRAINT IF EXISTS FK_ITEM_ON_OWNER;
ALTER TABLE item
    ADD CONSTRAINT FK_ITEM_ON_OWNER FOREIGN KEY (owner_id) REFERENCES app_user (id);
ALTER TABLE item
    ADD CONSTRAINT FK_ITEM_ON_ITEM_REQUEST FOREIGN KEY (item_request_id) REFERENCES item_request (id);
